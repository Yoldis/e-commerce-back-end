info:
  name: Seed
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/api/seed/generate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
